#ifndef __MYTYPES_HPP__
#define __MYTYPES_HPP__
#include <vector>
#include <map>
#include <cstring>
typedef std::vector<std::pair<size_t, size_t> > arguVector_t;
typedef std::vector<arguVector_t> pipeSeparatedArgus_t;
//typedef std::map<std::string, std::string> varMap_t;

#endif
